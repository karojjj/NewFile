package com.syntax.class18;

public class ClassTask1 {

	int sumArray(int [] array) { //Create a method that will accept an array as parameters 
		int sum=0;
		for (int num:array) {
			sum+=num;
		}
		return sum; //and will return a sum
	}
	public static void main(String[] args) {
		// Create a method that will accept an array as parameters 
		//and will return a sum
		// of all elements from that array. 
		//Method should be visibly only within same
		// package (mean should be default access modifiers) and accessible by the creating an instance of the class.
 
		
	}

}
