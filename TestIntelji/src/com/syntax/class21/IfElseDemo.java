package com.syntax.class21;

public class IfElseDemo {
    public static void main(String[] args) {
        int number=10;
        //if number is greater than 10 we use

    }

       int maxNumber ( int num1, int num2) {

//num1 > num2 ? num1 : num2; mean if num1 is bigger than num2 it will print num1 otherwise num2
           int maxNum = num1 > num2 ? num1 : num2;
           return maxNum;
       }}