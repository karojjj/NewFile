package com.syntax.class17New;

public class TypesofVariables {

	public static void main(String[] args) {
		/*
		 * there are three types of variables :A variable declared inside the body of
		 * the method is called local variable. The variables which are declare inside a
		 * method or constructor or blocks those variables are called local variables.It
		 * is possible to access local variables only inside the method or constructor
		 * or blocks only, it is not possible to access outside of method or constructor
		 * or blocks. Local variables memory allocated when method starts & memory
		 * released when method completed
		 * local variable: if variable is defined inside a 
		 */

	}

}
